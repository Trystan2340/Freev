# FreevBrain v7 — Dépendances
# Cœur (Python standard library uniquement) :
# - re, json, math, random, os, sys, time, datetime, pathlib, collections, heapq, zlib, base64, urllib

# DeepSeek V4 Pro via NVIDIA API
openai

# Optionnel : Moteur Vocal (voice.py / main.py --vocal)
pyttsx3
SpeechRecognition
pyaudio
pycaw
