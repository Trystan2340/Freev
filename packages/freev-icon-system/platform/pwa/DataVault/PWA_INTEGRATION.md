# DataVault — PWA

`manifest.webmanifest` est un manifeste autonome valide utilisable comme base.
Adapte `id`, `start_url` et `scope` à l'URL réelle de ton application si nécessaire.

Les trois icônes incluses couvrent 192px, 512px et Maskable 512px.
